# Linked Supabase Project

Project name: `nexoraadmi007-a11y's Project`

Project ref:

```text
zszgfzpjnqiyqnrjnqtq
```

Region:

```text
eu-central-2
```

Database host:

```text
db.zszgfzpjnqiyqnrjnqtq.supabase.co
```

Project URL:

```text
https://zszgfzpjnqiyqnrjnqtq.supabase.co
```

Required secrets still need to be supplied manually:

- `SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `DATABASE_URL`

Connectivity note:

Direct database host `db.zszgfzpjnqiyqnrjnqtq.supabase.co:5432` was not reachable from the local environment. If this persists, use the Supabase pooled connection string from:

```text
Project Settings -> Database -> Connection string -> Transaction pooler
```

Set that pooled URI as `DATABASE_URL`.
