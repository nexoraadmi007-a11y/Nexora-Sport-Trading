# GitHub Status

The GitHub connector is installed, but it currently reports:

```text
installed accounts: 0
accessible repositories: 0
```

To push this project, connect a GitHub account/repository through the GitHub connector or provide:

- `GITHUB_REPOSITORY_URL`
- `GITHUB_PERSONAL_ACCESS_TOKEN`

Once a repo is available, initialize the local Git repository and push the Phase 1 foundation.
