{
  "name": "@nexora/api",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "ts-node src/main.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nexora/shared": "0.1.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
